name: novgov

networks:
  internal:
    driver: bridge

volumes:
  db-data:
  # Cache disque L2 de géométrie tactique (murs/grille/portes/fenêtres par bâtiment, voir
  # TacticalGridBuilder.cs) — survit à un redéploiement (contrairement au système de fichiers du
  # conteneur, recréé à chaque "docker compose up -d"). Sans ce volume, un redéploiement payait à
  # nouveau la reconstruction complète depuis la scène Unity (fetch OSM + bake NavMesh pour une
  # tuile GPS réelle, jusqu'à ~60s) pour chaque carte déjà vue avant le redémarrage (2026-08-30).
  # Utile même à une seule instance : conservé au travers des redéploiements du conteneur.
  tactical-grid-cache:

services:
  # ---------------------------------------------------------------------
  # PostgreSQL (moteur Supabase) — jamais exposé sur l'hôte.
  # ---------------------------------------------------------------------
  db:
    image: supabase/postgres:15.6.1.146
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      JWT_SECRET: ${JWT_SECRET}
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/99-schema.sql:ro
    networks: [internal]
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ---------------------------------------------------------------------
  # GoTrue — Authentification (JWT, signup/login/refresh). Interne uniquement.
  # ---------------------------------------------------------------------
  auth:
    image: supabase/gotrue:v2.196.0
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      API_EXTERNAL_URL: ${SITE_URL}
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_NAMESPACE: auth
      DATABASE_URL: postgres://supabase_auth_admin:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      GOTRUE_SITE_URL: ${SITE_URL}
      GOTRUE_URI_ALLOW_LIST: "*"
      GOTRUE_DISABLE_SIGNUP: "false"
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_EXP: 3600
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated
      GOTRUE_MAILER_AUTOCONFIRM: ${GOTRUE_MAILER_AUTOCONFIRM}
    networks: [internal]

  # ---------------------------------------------------------------------
  # PostgREST — API REST auto-générée sur Postgres (lectures non critiques
  # côté client : profil, historique de parties). Interne uniquement.
  # ---------------------------------------------------------------------
  rest:
    image: postgrest/postgrest:v12.2.3
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: postgres://authenticator:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PGRST_DB_SCHEMAS: public
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${JWT_SECRET}
      PGRST_SERVER_PORT: 3000
    networks: [internal]

  # ---------------------------------------------------------------------
  # Serveur de jeu Unity Headless — AUTORITAIRE. UNE SEULE instance (2026-08-30, remplace le pool
  # de 3 instances du même jour) : depuis "Option B" + le pool de threads (voir MatchSessionManager.
  # RunExecutionPhasePure), UN processus fait tourner des centaines/milliers de parties Deathmatch/
  # Zone de Contrôle EN PARALLÈLE — le pool de 3 processus ne servait plus qu'à payer 2x le coût de
  # base d'un moteur Unity (RAM/CPU) sans gain de capacité réel. La table "server_instances"
  # (schema.sql §7, mise à jour toutes les 2s via ReportInstanceStatus) et la logique côté client
  # (MultiplayerMatchController.ConnectToGameServerCoroutine, "choisir une instance libre") sont
  # conservées telles quelles — elles fonctionnent déjà correctement avec une seule ligne dans la
  # table, aucun changement nécessaire pour repasser à plusieurs instances plus tard si le besoin
  # (résilience, pas capacité) s'en faisait sentir : copier-coller ce bloc avec un nouveau port et un
  # nouvel INSTANCE_ID suffirait.
  # ---------------------------------------------------------------------
  game-server-1:
    build:
      context: ./game-server
    image: novgov-game-server:latest
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      rest:
        condition: service_started
    environment:
      JWT_SECRET: ${JWT_SECRET}
      GAME_PORT: 7777
      REST_URL: https://novgov.com/rest/v1
      SERVICE_ROLE_KEY: ${SERVICE_ROLE_KEY}
      INSTANCE_ID: game-server-1
      PUBLIC_PORT: 7777
    volumes:
      - tactical-grid-cache:/root/.config/unity3d/DefaultCompany/Novgov/TacticalGridCache
    ports:
      - "7777:7777"
    networks: [internal]

  # ---------------------------------------------------------------------
  # Nginx — reverse proxy public (80/443) vers auth/rest, TLS.
  # ---------------------------------------------------------------------
  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on: [auth, rest]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80"
      - "443:443"
    networks: [internal]
